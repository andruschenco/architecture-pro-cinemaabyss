{{- if .Values.proxyService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.proxyService.name | default "proxy-service" }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.proxyService.name | default "proxy-service" }}
    app.kubernetes.io/name: {{ include "cinemaabyss.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: {{ .Values.proxyService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.proxyService.name | default "proxy-service" }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: {{ .Values.proxyService.name | default "proxy-service" }}
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      containers:
        - name: {{ .Values.proxyService.name | default "proxy-service" }}
          image: "{{ .Values.proxyService.image.repository }}:{{ .Values.proxyService.image.tag }}"
          imagePullPolicy: {{ .Values.proxyService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.proxyService.service.targetPort }}
          env:
            - name: PORT
              value: "{{ .Values.proxyService.service.targetPort }}"
            - name: MONOLITH_URL
              value: "http://monolith:8080" #
            - name: MOVIES_SERVICE_URL
              value: "http://movies-service:8081"
            - name: EVENTS_SERVICE_URL
              value: "http://events-service:80"
            - name: GRADUAL_MIGRATION
              value: "true"
            - name: MOVIES_MIGRATION_PERCENT
              valueFrom:
                configMapKeyRef:
                  name: cinemaabyss-config
                  key: MOVIES_MIGRATION_PERCENT
          resources:
            limits:
              cpu: {{ .Values.proxyService.resources.limits.cpu }}
              memory: {{ .Values.proxyService.resources.limits.memory }}
            requests:
              cpu: {{ .Values.proxyService.resources.requests.cpu }}
              memory: {{ .Values.proxyService.resources.requests.memory }}
          readinessProbe:
            httpGet:
              path: {{ .Values.proxyService.probes.readiness.path }} # path: /health
              port: {{ .Values.proxyService.service.targetPort }}
#            initialDelaySeconds: 5
#            periodSeconds: 5
            initialDelaySeconds:  {{ .Values.proxyService.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.proxyService.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.proxyService.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.proxyService.probes.readiness.failureThreshold }}
          livenessProbe:
            httpGet:
#              path: /health
              path: {{ .Values.proxyService.probes.liveness.path }}
              port: {{ .Values.proxyService.service.targetPort }}
            initialDelaySeconds: 35
            periodSeconds: 15

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.proxyService.name | default "proxy-service" }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.proxyService.name | default "proxy-service" }}
    app.kubernetes.io/name: {{ include "cinemaabyss.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.proxyService.service.type }}
  selector:
    app: {{ .Values.proxyService.name | default "proxy-service" }}
  ports:
    - protocol: TCP
      port: {{ .Values.proxyService.service.port }}
      targetPort: {{ .Values.proxyService.service.targetPort }}
  {{- end }}